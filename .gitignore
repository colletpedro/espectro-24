# Segredos
.env

# Python
venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Cache de rede reconstruível (SPEC.md §3[B]) — não é código-fonte
resultado/cache/

# Checkpoint de execução do harness de lote (SPEC.md §3[H]) — estado local
# de UMA rodada, não um artefato a versionar (ao contrário de dados/bruto/,
# que É versionado: é o superset reusável, §3[B']).
dados/lote/

# Config local do Claude Code (launch.json do servidor de preview, etc.).
# Ferramenta de quem está editando, não do projeto — nunca entra no repo.
.claude/

# [v1.9.29] cache de rede do TMDB, criado por scripts/enriquecer_ficha.py
# (mesmo estatuto de resultado/cache/ acima: artefato de rede, nunca fonte)
dados/cache/

# macOS
.DS_Store

# [2026-09-14] Documentos de ESTUDO ficam fora do git (política da sessão:
# "estudo untracked", ABERTO.md). Estes três diretórios entraram por engano
# em 594786f e saíram no commit seguinte; a entrada impede que reentrem por
# um `git add` amplo. Escopo DELIBERADAMENTE estreito: os outros subdiretórios
# de docs/arquivo-de-estudos/ têm arquivos rastreados desde 85a8f11, e um
# padrão para a pasta inteira passaria a ignorar arquivos novos neles.
docs/arquivo-de-estudos/galeria-de-stills/
docs/arquivo-de-estudos/piloto-expansao/
docs/arquivo-de-estudos/revisao-condicoes/
# [2026-09-15] Experimento de briefing (ABERTO.md C14.18) — mesma política,
# mesmo escopo estreito.
docs/arquivo-de-estudos/experimento-briefing/
